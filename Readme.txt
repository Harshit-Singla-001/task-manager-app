A C++ OS(Operating Systems) project.

A Command Line Interface Task Manager(NO GUI) 

How To Run the code:-
    1. Open the folder in Ide(vscode).
    2. Run this Following Command --> g++ main.cpp ProcessList.cpp -o task_manager -lpsapi
    3. This will generate a exe file named task_manager.exe,run it to get you task manager running.
